# ┌──────────────────────────────────────────────────────┐
# │ SQLite + InMemoryCache 전환으로 Docker 서비스 비활성화    │
# │ 마이그레이션 시에만 PostgreSQL 컨테이너를 일시적으로 사용    │
# │ docker compose up postgres -d                         │
# └──────────────────────────────────────────────────────┘

services:
  # ================================================================
  # PostgreSQL 17 + pgvector (마이그레이션 전용 - migration 프로필에서만 실행)
  # 실행 방법: docker compose --profile migration up postgres -d
  # ================================================================
  postgres:
    image: pgvector/pgvector:pg17
    container_name: trading-postgres
    environment:
      POSTGRES_USER: trading
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD must be set in .env}
      POSTGRES_DB: trading_system
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trading"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - trading
    profiles:
      - migration

  # ================================================================
  # Redis 7 (마이그레이션 전용 - migration 프로필에서만 실행)
  # 실행 방법: docker compose --profile migration up redis -d
  # ================================================================
  redis:
    image: redis:7-alpine
    container_name: trading-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - trading
    profiles:
      - migration

  # ================================================================
  # FastAPI Dashboard Server -- 호스트에서 직접 실행으로 이전
  # Claude CLI (local SDK) 접근을 위해 Docker에서 제거되었다.
  # 실행 방법: scripts/start_api.sh 또는 scripts/start_dashboard.py
  # ================================================================
  # api:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: trading-api
  #   ports:
  #     - "${API_PORT:-9501}:9501"
  #   env_file:
  #     - .env
  #   environment:
  #     DB_HOST: postgres
  #     DB_PORT: "5432"
  #     REDIS_HOST: redis
  #     REDIS_PORT: "6379"
  #     DOCKER_MODE: "1"
  #     MLX_ENABLED: "0"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   restart: always
  #   volumes:
  #     - ./data:/app/data
  #     - ./data/strategy_params.json:/app/data/strategy_params.json
  #   networks:
  #     - trading

  # ================================================================
  # Production 전용 서비스 (내부 네트워크, 포트 미노출)
  # docker compose --profile prod-internal up -d 로 실행
  # SQLite 전환 후 마이그레이션 시에만 사용 (migration 프로필 포함)
  # ================================================================
  postgres-prod:
    image: pgvector/pgvector:pg17
    container_name: trading-postgres-prod
    environment:
      POSTGRES_USER: trading
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: trading_system
    expose:
      - "5432"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trading"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - trading-internal
    profiles:
      - prod-internal
      - migration

  redis-prod:
    image: redis:7-alpine
    container_name: trading-redis-prod
    expose:
      - "6379"
    volumes:
      - redis_data_prod:/data
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - trading-internal
    profiles:
      - prod-internal
      - migration

volumes:
  postgres_data:
  redis_data:
  postgres_data_prod:
  redis_data_prod:

networks:
  trading:
    driver: bridge
  trading-internal:
    driver: bridge
    internal: true
