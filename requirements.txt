# ── 코어 서버 ──
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.5
sqlalchemy==2.0.36
alembic==1.14.1
aiosqlite==0.20.0
# asyncpg==0.30.0  # PostgreSQL 전용 - SQLite 전환으로 불필요
# psycopg2-binary==2.9.10  # PostgreSQL 전용 - 마이그레이션 후 제거 가능
# redis==5.2.1  # Redis 전용 - 인메모리 캐시로 대체

# ── 네트워크 ──
aiohttp==3.11.11
httpx==0.28.1
websockets==14.1
uvloop==0.22.1

# ── AI / ML ──
anthropic==0.42.0
numpy==2.2.6
hmmlearn==0.3.3
# lightgbm, scikit-learn, optuna: ML 파이프라인에서 lazy import한다
# 미설치 시 ImportError로 안내하며 핵심 매매에는 영향 없다
# lightgbm==4.5.0
# scikit-learn==1.6.1
# optuna==4.1.0

# ── 로컬 LLM (GGUF) ──
# llama-cpp-python: local_llm.py에서 lazy import한다 (GGUF 모델 추론용)
# 미설치 시 번역/분류 기능 비활성화, 핵심 매매에는 영향 없다
# llama-cpp-python==0.3.8
mlx-lm==0.21.2

# ── 번역 (MarianMT) ──
sentencepiece==0.2.0

# ── 데이터/크롤링 ──
feedparser==6.0.11
beautifulsoup4==4.12.3
python-dotenv==1.0.1

# ── RAG ──
sentence-transformers==3.3.1
FlagEmbedding==1.3.3
chromadb==0.6.3

# ── 알림 ──
python-telegram-bot==21.10

# ── 테스트/정적분석 ──
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
mypy==1.14.1
ruff==0.8.6

# ── 미사용으로 제거된 패키지 (참고용) ──
# pydantic-settings==2.7.1  # 코드에서 미사용 (SecretVault로 대체)
# python-kis==1.0.0  # 코드에서 미사용 (직접 KIS API 호출)
# pandas==3.0.0  # 코드에서 미사용 (numpy로 대체)
# pandas-ta==0.4.71b0  # 코드에서 미사용 (직접 지표 계산)
# yfinance==0.2.51  # 코드에서 미사용 (KIS API로 대체)
# finvizfinance==1.3.0  # 코드에서 미사용
# investiny==0.7.0  # 코드에서 미사용
# apscheduler==3.10.4  # 코드에서 미사용 (asyncio 루프로 대체)
# praw==7.8.1  # 코드에서 미사용 (직접 HTTP 호출)
# playwright==1.49.0  # 코드에서 미사용
